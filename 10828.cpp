#include "myStack.h"
using namespace std;



/*
int main() {

	int num{ 0 };
	int arg{ 0 };
	int i = 0;
	string cmd{ "" };
	cin >> num;

	Vector_ vector;
	
	while (i < num) {
		cin >> cmd;
		if (cmd == "push") {
			cin >> arg;
			vector.push(arg);
		}
		else if (cmd == "top") {
			cout << vector.top() << endl;
		}
		else if (cmd == "size") {
			cout << vector.size() << endl;
		}
		else if (cmd == "empty") {
			cout << vector.empty() << endl;
		}
		else if (cmd == "pop") {
			cout << vector.pop() << endl;
		}
		++i;
	}


	return 0;
}
*/